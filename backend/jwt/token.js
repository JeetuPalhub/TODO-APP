// Make a token and store it in cookies
export const generateTokenAndsaveInCookies = (req, res) => {
    console.log("Token generated: ") // Right now, it just says token made, but doesn't really make one yet
}
